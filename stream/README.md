This resource does **not** stream the crown/halo models.

Keep your existing stream resources started (the folders that already contain these files):

- `crown_props.ytyp` plus all `*_crown.ydr` files
- `n93_halos.ytyp` plus all `halo_*.ydr` files

Starting the same `.ydr` files from two resources at once will break the props. Leave this folder empty.
